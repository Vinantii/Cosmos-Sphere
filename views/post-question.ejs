<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Post a New Question</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: radial-gradient(circle at top left, #0b0c10, #1a1f71);
      color: #fff;
      min-height: 100vh;
      padding-top: 70px;
    }

    header.navbar {
      background: rgba(0,0,0,0.85);
      backdrop-filter: blur(6px);
      position: fixed; top:0; width:100%; z-index:1000;
    }

    .container {
      max-width: 700px;
      margin: auto;
    }

    .form-card {
      background: linear-gradient(145deg, #1f1f3a, #0b0c10);
      border: 1px solid #3A50F0;
      border-radius: 12px;
      padding: 25px;
      box-shadow: 0 0 10px #3A50F0, 0 0 15px #8A2BE2;
      margin-top: 30px;
    }

    textarea {
      width: 100%;
      min-height: 120px;
      border-radius: 8px;
      padding: 10px;
      border: 1px solid #3A50F0;
      background: #0b0c10;
      color: #fff;
      resize: vertical;
    }

    .btn-glow {
      background: linear-gradient(45deg, #3A50F0, #8A2BE2);
      color: #fff;
      font-weight: 600;
      border-radius: 8px;
      box-shadow: 0 0 8px #3A50F0, 0 0 12px #8A2BE2;
      transition: all 0.3s ease;
    }

    .btn-glow:hover {
      transform: scale(1.05);
      box-shadow: 0 0 12px #fff, 0 0 18px #8A2BE2;
    }

    a.btn-glow { text-decoration: none; }
  </style>
</head>
<body>
  <!-- Header -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/landing">CosmoSphere</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><a class="nav-link" href="/view-questions">All Questions</a></li>
          <li class="nav-item"><span class="nav-link">Hello <%= currentUser.username %></span></li>
          <li class="nav-item"><a class="nav-link btn btn-outline-light ms-2" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="form-card">
      <h2>Post a New Question</h2>
      <p>Logged in as: <strong><%= currentUser.username %></strong></p>

      <form action="/post-question" method="POST">
        <textarea name="question" placeholder="Type your question here..." required></textarea>
        <div class="mt-3 d-flex gap-2">
          <button type="submit" class="btn btn-glow">Post Question</button>
          <a href="/view-questions" class="btn btn-glow">Back</a>
        </div>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
